# For non-conda setups. With conda, use LiCSBAS.yml instead:
#     conda env create -f LiCSBAS.yml
# Do not `conda install --file requirements.txt` into an existing
# environment: mixing its channel with conda-forge, and changing the python
# version in place, can make the solve take hours. Create a new environment
# from LiCSBAS.yml instead, which pins the channel to conda-forge.
#
# Mirror of the dependencies in LiCSBAS.yml (see there for the rationale
# behind the bounds and for the last verified version combination).
python>=3.12
astropy
beautifulsoup4
gdal>=3.6
h5py
matplotlib>=3.6
numpy>=2
pandas
psutil
python-dateutil
pyyaml
requests
shapely>=2
statsmodels
threadpoolctl
# Optional, for the --gpu option of steps 13 and 14 (needs an NVIDIA GPU):
#cupy>=14
